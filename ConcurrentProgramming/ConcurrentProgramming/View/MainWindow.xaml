<Window x:Class="ConcurrentProgramming.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConcurrentProgramming"
        xmlns:viewModel="clr-namespace:Presentation.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <viewModel:ViewModel />
    </Window.DataContext>
    
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <TextBlock VerticalAlignment="Center" TextWrapping="Wrap" Text="How many balls to add/create"></TextBlock>
        
        <TextBox Grid.Column="1" Margin="10.0" VerticalAlignment="Center" Text="{Binding BallCount, UpdateSourceTrigger=PropertyChanged, TargetNullValue=0}"/>
        
        <Button Grid.Column="2" Margin="5.0" Content="Add" Command="{Binding AddCommand}"/>
        
        <Button Grid.Column="3" Margin="5.0" Content="Create" Command="{Binding CreateCommand}"/>
        
    </Grid>
</Window>
